# Peplink-Wifi-Toggle-V2
updated version with cleaner code and bug fixes

------------------------------------------------------------------------

build with:
docker buildx build   --platform linux/amd64,linux/arm64,linux/arm/v7   -t DOCKER_HUB_USERNAME/DOCKER_HUB_REPOSITORY:stable   --push .

------------------------------------------------------------------------

link to docker repo:
https://hub.docker.com/repository/docker/audriuspeplink/peplinkgpswifishutdown/general

------------------------------------------------------------------------
